"""Пакет тестов backend.

Файл нужен не для оформления: в site-packages бывает установлен
посторонний пакет с именем `tests` (его кладут туда библиотеки с
ошибкой в упаковке). Каталог без __init__.py считается лишь частью
пространства имён, а обычный пакет побеждает его независимо от порядка
sys.path — и тогда `from tests.llama_server_stub import ...` разрешается
в чужой пакет и падает. С этим файлом каталог становится обычным
пакетом и находится первым.
"""
