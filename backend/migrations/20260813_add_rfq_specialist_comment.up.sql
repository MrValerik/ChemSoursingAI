-- Комментарий закупщика к позиции (ТЗ, функция 1: «форма ввода ... и
-- комментариев специалиста»). Внутренняя заметка: в письмо поставщику
-- не попадает.
ALTER TABLE rfqs
    ADD COLUMN IF NOT EXISTS specialist_comment TEXT;
