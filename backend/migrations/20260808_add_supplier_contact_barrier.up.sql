-- Почему у компании нет канала связи.
--
-- «Нет контакта» закупщик читает как «компания недостижима» и вычёркивает
-- её. На деле у Ningbo Inno адрес на странице есть — просто подменён
-- Cloudflare на «[email protected]», и написать можно, открыв страницу
-- руками. У других вместо адреса стоит форма обратной связи: адреса нет
-- ни у кого, но путь к компании всё равно есть.
--
-- Значения: obfuscated (адрес скрыт), form (только форма на сайте).
ALTER TABLE suppliers ADD COLUMN IF NOT EXISTS contact_barrier VARCHAR(32);
