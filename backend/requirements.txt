fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
httpx==0.27.2
# HTTP/2 для загрузки страниц: браузеры ходят по h2, часть сайтов это проверяет.
h2==4.1.0
PyJWT==2.9.0
python-dotenv==1.0.1
cryptography==46.0.3
# Разбор multipart/form-data: загрузка файла со списком позиций. Ставится
# явно — FastAPI и uvicorn его за собой не тянут, и без него загрузка
# отвечает ошибкой только в собранном образе, а локально работает.
python-multipart==0.0.22
# Разбор XLSX при импорте списка позиций. Чистый Python, без системных
# зависимостей. Читается с data_only=True: нужны посчитанные значения
# формул, а не сами формулы.
openpyxl==3.1.5
# Текстовый слой PDF (CoA/TDS). Чистый Python, без системных зависимостей.
pypdf==5.1.0
# OCR сканов: Tesseract и растеризация PDF (системные пакеты — в Dockerfile).
pytesseract==0.3.13
pdf2image==1.17.0
pillow==11.0.0
