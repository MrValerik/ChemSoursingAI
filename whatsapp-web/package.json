{
  "name": "chemsource-whatsapp-web-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated WhatsApp Web gateway for ChemSource AI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check incoming-event.js && node --check message-id.js && node --check session-files.js && node --check sender-number.js",
    "test": "node --test incoming-event.test.js message-id.test.js session-files.test.js sender-number.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "4.21.2",
    "qrcode": "1.5.4",
    "whatsapp-web.js": "1.34.7"
  }
}
